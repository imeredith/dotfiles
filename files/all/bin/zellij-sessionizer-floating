#!/usr/bin/env bash

set -euo pipefail

cleanup() {
  if [[ -n ${ZELLIJ_PANE_ID:-} ]]; then
    zellij action close-pane --pane-id "$ZELLIJ_PANE_ID" >/dev/null 2>&1 || true
  fi
}

trap cleanup EXIT

zellij-sessionizer "$@"
